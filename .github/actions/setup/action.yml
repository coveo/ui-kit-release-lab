name: 'Setup & Build'
description: 'Setup pnpm, install dependencies & build'
inputs:
  skip-build:
    description: 'Skip the build step'
    required: false
    default: 'false'
runs:
  using: composite
  steps:
    - name: Setup pnpm
      uses: step-security/action-setup@3d419c73e38e670dbffe349ffff26dd13c164640 # v4.2.0
      with:
        run_install: false

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'pnpm'

    - name: Install dependencies
      run: pnpm install --frozen-lockfile
      shell: bash

    - name: Build
      if: inputs.skip-build != 'true'
      run: pnpm run build
      shell: bash
